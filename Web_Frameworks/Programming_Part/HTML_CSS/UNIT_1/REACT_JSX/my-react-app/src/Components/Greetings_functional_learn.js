import React from 'react';

const Greeting=({name})=>{ //function Greeting
    return (
        <div>
            <h1> Hello, (name)! </h1>
            <p>Welcome To React</p>
        </div>

    );
};
export default Greeting;