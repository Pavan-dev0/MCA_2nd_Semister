Discriminative classifier is a model that focuses on finding the boundary b/w the different classes 

We use the soft margin to determine the location of the threshold
When we use the soft margin classifier aka a support vector classifier to classify observation