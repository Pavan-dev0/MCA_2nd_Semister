
A Table is a matrix of cells given using <table> tag
a border attribute in <table> 
<td> is table data
<th> is table header
<tr> is table row 

ColSpan must be set in the <th> tag to specify the level that make the cells to make it combined together
Rowspan combines the rows together

Align is the attribute controls the horizontal placement of contents in a table cell