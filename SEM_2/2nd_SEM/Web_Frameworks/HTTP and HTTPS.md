**Http** is a protocol used for transferring web pages
**https** is a secure version of HTTP

## HTTP
- its a application layer protocol 
- uses port 80

## HTTPS 
- uses SSL/TLS encryption 
- Port 443 

## HTTP Communication Methods
Http is sent across a http plain text


### HTTPS Communication Methods
send data in encrypted format

# FTP File transfer protocol
Used to transfer files between client and server
Application Layer protocol

- Uses the port 21

--- 
# SMP,POP3,IMAP

SMTP is a simple mail transfer protocol 
uses the port 25/87

pop3 is (post office protocol)
used to recieve emails
- uses port 110
emails removed from server after download

**IMAP** internet message acess protocol 

--- 

# DNS
Dns is the domain name system names to IP address "https://www,google.com"
here www.google.com is the DNS

DNS Components are
- Dns Resolver [// Resolver will keep on checking the server]
- Root Server
- Tld Server
- Authoritative server 
its like 
example:- 
Browser->Resolver->Root->Tld->Auth->IPAddr->Usr

# Domain Name Conversion
- User Enter the domain name  


# URL 
URL is nothing but the uniform resource locator
locates resource on internet

## Cookies and Cache
They are stored in browser data 
- cache stores static website
- cookies are small files storing user specific data 
- clearing cookies resolves loading issues but may log out of sites

## Static Vs Dynamic Websites

## Internet Services

## Internet Security

## Internet Engineering Task Force

##ICANN 
